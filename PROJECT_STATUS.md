# Research Exercise — Mission Control

> Read this first in any new chat about the figures draft. It replaces re-deriving context from the long August conversation. Update the status tables here at the end of each working session.

> **`DATA_CATALOG.md`** (in the project folder, canonical copy in this repo) is the source of truth for every CT scan/sample: what exists, where, preprocessing status, model/checkpoint, and validity with explicit reasoning. Read it before any prompt does its own rediscovery of what scans are available. It also states the corrected sample-inclusion rule (exclude only for channel collapse, an unfixed preprocessing defect, or a confirmed wrong model — NOT merely for differing in magnitude from another sample). **Note (2026-08-27): its written status column is POM-focused; a volume marked not-in-use for POM can still be valid for Track E's binary/pore-channel work — see the Track E row and `connectivity_replicate_expansion_prompt.md`.**

> **Correction (2026-08-29):** the Bnei Re'em specimen picture below was wrong in one specific way and has been fixed in `DATA_CATALOG.md` — read that fix before relying on anything below that mentions Bnei Re'em specimen counts. Short version: Bnei Re'em is **2 physical specimens**, not 1 with an uncertain/unreconstructed second candidate. "Canonical" (`bnei_reem_fresh_bnei_reem_i4`) is the reconstruction (~June/July 2026) of one of those two specimens (raw core `bnei_reem_samp_2`, no dot). The other specimen is `bnei_reem_samp_2.0` (`_recropped`, POM elevated not collapsed). **This does not change the POM conclusion** — Bnei Re'em POM analysis stays n=1 (canonical only), since Specimen B doesn't pass the POM gate — but it does mean Bnei Re'em has real n=2 physical specimens available for structural/pore-only work (Track E, A2), more than previously documented. **Analysis-tier policy (Rony, 2026-08-29), now pinned in `DATA_CATALOG.md`:** every reconstruction with a valid pore channel is in scope for structural/topology metrics regardless of POM status; POM-inclusive analysis stays restricted to exactly 3 volumes — Bnei Re'em canonical, Mishmar 5.85 µm, Mishmar 8.8 µm.

## POM cross-soil comparison — status summary (2026-08-26, count corrected 2026-08-29)

**Settled direction: Bnei Re'em stays at n=1 for POM** (the known, already-trusted canonical volume, segmentation output `nlm_volume` — the one Rony has been annotating against). ~~Rony ruled out `bnei_reem_samp_2_0`/`_recropped` and `bnei_reem_samp_2` (no dot, never reconstructed) as replicate candidates~~ **Correction (2026-08-29): `bnei_reem_samp_2` (no dot) was never a separate, unreconstructed candidate — it's the same physical core as canonical. Specimen B (`bnei_reem_samp_2.0`) is the only other physical specimen, and it fails the POM gate (elevated, not collapsed). Bnei Re'em correctly stays n=1 for POM.** Rony does not expect further Bnei Re'em scans for POM purposes. Mishmar stands at genuine n=2 (native 5.85 µm + a second ~8.8 µm sample, both label-downsampled to ~15 µm).

**How we got here (condensed):** the original 1-vs-1 comparison's shape/spatial metrics are solid regardless of anything below — volume-weighted median POM diameter (690.2 µm Bnei Re'em vs. 734.0 µm Mishmar), largest-object share (17.1% vs. 45.1%), Clark-Evans spatial clustering (R=0.640 vs. R=0.818). Getting a trustworthy *clustering/archetype* comparison on top of that took several rounds: a native-15µm Mishmar rescan attempt failed outright (POM channel collapsed, model failure — see `Cu011_samp_2` in `DATA_CATALOG.md`); two computational resolution-matching ablations (label-downsample vs. image-downsample-then-predict) disagreed by ~54%, revealing the segmentation model isn't robust to how a 15 µm input is produced (image-then-predict was dropped for this reason); two clustering script versions computed sphericity differently and gave non-comparable archetype counts on identical data; and a `bnei_reem_samp_2_0` candidate replicate was chased for several sessions (root-caused to a missing crop step, fixed, still 9.3× canonical POM) before Rony corrected the exclusion logic itself (magnitude difference ≠ segmentation failure — only channel collapse, an unfixed preprocessing defect, or a wrong model justify exclusion) and ultimately decided not to use it, settling on n=1 instead of chasing it further.

**Current pinned methodology** (`pom_final_clustering_prompt.md`, run 2026-08-26 as `final`): marching-cubes sphericity (one fixed script version, no more mixing methods), PCA elongation/flatness, a minimum-resolvability cutoff (≥20 voxels / ~300 µm across an object's equivalent diameter) before any object enters shape-feature extraction, and a scaler fit only on the 3 volumes actually being compared. Result: Bnei Re'em n=1 vs. Mishmar n=2, 6 shape archetypes found. **One finding looks real and reproducible**: two archetypes (small elongated objects; small equant/blocky objects) are present in *both* Mishmar replicates but *absent entirely* from Bnei Re'em — the rest of the archetype comparison is noisy given how few objects survive the resolvability cutoff (Bnei Re'em 76/2709 objects kept, Mishmar reps 19/1902 and 60/3479).

**ROI-expansion run completed 2026-08-29/30** (`pom_roi_expansion_and_final_clustering_prompt.md`, outputs in `05_evaluation/psd/pom_analysis_20260829_roi_expansion/`, full narrative in that folder's `final_report.md`): checked for extra raw volume beyond each crop, enlarged where safe. Both Mishmar volumes grew substantially (1000³→1216³ and 1000³→1614³) and passed every sanity check. Bnei Re'em's enlarged 722³ crop caused its POM channel to collapse (0.819%→0.254%, below the 0.3% floor) — a genuine channel-collapse per the exclusion rule, not a magnitude difference — so it automatically fell back to its existing, already-trusted 650³ crop per the prompt's own rule.

**Verdict: the prior run's one "reproducible" archetype finding does not hold up and is retired.** At the pinned 20-voxel cutoff, the much larger Mishmar object counts (Mishmar rep 2 alone: 60→294 kept objects) collapsed the archetype structure from 6 archetypes down to 2, and in that simpler structure Bnei Re'em became numerically identical to Mishmar rep 1 — it's Mishmar rep 2 that stands apart now, the opposite of the prior story. This held at cutoff=15 too; only a partial, single-archetype echo reappeared at cutoff=25 (back to 6 archetypes, very small per-archetype n). Silhouette scores throughout (0.30–0.38) are weak, and the number of archetypes recovered is itself unstable across cutoffs (2, 2, 6) — a second-order robustness problem independent of the specific finding. **Decision (2026-08-30): the shape/archetype clustering angle for POM is retired as inconclusive — not stable enough to support any claim of a real compositional difference between the soils, given tiny post-cutoff object counts (tens to low hundreds) and Bnei Re'em stuck at n=1.** This does not affect the original, clustering-independent descriptive results (see next paragraph), which remain the reportable POM findings.

**POM work going forward, without clustering:** the solid, reportable results are volume-weighted median POM diameter (690.2 µm Bnei Re'em vs. 734.0 µm Mishmar), largest-object share (17.1% vs. 45.1%), and Clark-Evans spatial clustering (R=0.640 vs. R=0.818) — plain descriptive/spatial statistics, unaffected by any of the clustering instability above. A short note retiring the clustering angle still needs to go into the v7 draft next to comment 3 (see 'Open comments' below).

**Interface-ratio metrics (Track B step 2a) — done 2026-08-30, resolution bug found and fixed 2026-08-31** (`pom_interface_metrics_prompt.md` then `pom_interface_metrics_resolution_fix_prompt.md`; original outputs in `05_evaluation/psd/pom_analysis_20260830_interface_metrics/`, resolution-matched re-run in `05_evaluation/psd/pom_analysis_20260831_interface_metrics_resmatched/` — **use the 08-31 numbers going forward**, full before/after narrative in the 08-30 folder's `final_report.md` under "Resolution correction"). Reused the same 3 volumes/crops and each one's own pinned noise-floor cutoff (not the ROI-expanded crops). Part 1's Bnei Re'em voxel-face POM–pore contact fraction reproduced the pinned 0.6423 value exactly (diff=0.0) in both runs. **Bug (found 08-31): the 08-30 run's "Mishmar mean±SE, n=2" mixed a 5.85 µm replicate (`mishmar_native`, not downsampled) with a ~15 µm replicate (`mishmar_sample2`) — a resolution confound (SSA/IAD are voxel-size-sensitive), producing a spurious ~49% SSA gap between the two "replicates". Fixed by swapping in the already-existing label-downsampled-to-15µm version of the same native-5.85µm sample (reproduced its own pinned reference value exactly, diff=0.0, before trusting it).** **Every directional conclusion survived the fix unchanged**, with the headline numbers updated slightly: **interfacial area density (IAD) is 1.6–2.1× higher in Mishmar than Bnei Re'em for both POM–pore (0.251±0.042 vs 0.156 mm²/mm³) and POM–matrix (0.230±0.020 vs 0.110 mm²/mm³) interfaces, even though POM-intrinsic specific surface area (SSA) is statistically indistinguishable between the soils (30.6±4.4 vs 33.2 mm²/mm³ total)**. Largest-object POM-pore interface share: 38.0±2.1% Mishmar vs 13.0% Bnei Re'em (top-5 54.9±3.8% vs 33.9%) — still proportionally more concentrated than the existing POM-*volume* largest-object share (45.1% vs 17.1%). Even after resolution-matching, the two Mishmar replicates' SSA still differ by ~34% (down from ~49%) — real between-replicate variability, not an artifact, and exactly what the n=2 SE bars are meant to carry. **One finding worth flagging explicitly (unaffected by the resolution fix):** marching-cubes total surface area came out consistently *smaller* than the naive voxel-face-count total (opposite of what the prompt's own literature summary predicted) across all volumes; verified via synthetic-sphere ground truth that this is the mathematically correct direction (voxel-face counting overestimates true surface area by ~50%, marching cubes by only ~8–9%) and not a bug. This closes out the last open POM/Track-B item (see 'Open comments' below, comment 3).

**Note on scope (2026-08-27, specimen picture corrected 2026-08-29):** all of the above (Bnei Re'em settled at n=1 for POM) is specific to **POM/3-class work**. Track E (connectivity, binary pore mask only) does not need a valid POM channel, so `bnei_reem_samp_2_0_recropped`'s plausible pore fraction (28.3%, no collapse pattern) makes it a usable Track E replicate candidate even though it's excluded from POM work — see the Track E row and `connectivity_replicate_expansion_prompt.md`. That gives Bnei Re'em 2 reconstructions (across its 2 physical specimens) for structural/pore-only work.

## The project in one paragraph

Research exercise: impact of soil structure on microbial aspects. Three soils — Rehovot (brown-red sand), Bnei Re'em (Vertisol), Mishmar HaNegev (Loess). Lab side: respiration (NaOH traps, 15-day incubation, jar L4 excluded as outlier), fumigation-extraction Cmic (paired to respiration jars), bulk density (two methods), texture/CaCO₃ pending. Imaging side: microCT + nnUNet segmentation (deployed label convention: pore=5, POM=2 — NOT dataset_info.json; Rehovot is binary pore/solid, no POM), PSD via local thickness, distance-to-target topology metrics, pore-network connectivity (Track E). Master document: **`figures draft - updated v7.docx`** (latest — edit from v7 only; older versions are history).

## Non-negotiable standards (mentor: dr. Maoz Dor)

1. **No SD anywhere. SE only.** Where SE is meaningless (voxel-wise metrics, n≈10⁸), report **mean (median)** instead. Already applied in v5+ Tables 1–2.
2. A graph is not finished without statistics: omnibus p (+ effect size η²) printed on the figure, Tukey/LSD compact letters, plot EMM ± SE. Full text: `Maoz_Figure_Standard.md`.
3. Figure style: use the **`soil-figures-style`** skill (saved in Cowork) — fixed soil colors, ordering, and stats-annotation conventions.

## Work split (decided 2026-08-22)

| Track | What | Where | Status |
|---|---|---|---|
| A | PSD bin coarsening for multiple comparison — literature check | Subagent | **Done** — `psd_binning_recommendation.md`; bins merged into `fig4_psd_inference_prompt.md`. **A2 (pore-PSD resolution confound):** Mishmar (5.85 µm) vs. others (15 µm) makes whole-distribution medians resolution-confounded; restrict cross-soil inference to ≥31 µm functional classes, use the native 15 µm Mishmar scan's **pore channel** (valid, see "Scans note") as the robustness check — done, see Fig 4b in Document state below. **A2 extended to POM (2026-08-22 → 08-26): see the condensed "POM cross-soil comparison — status summary" section near the top of this file** for the full current state and pinned methodology (specimen count corrected 2026-08-29 — see the correction note at the top of this file) — that section supersedes the old blow-by-blow narrative that used to live here. One-line history for context: chased a native-15µm Mishmar rescan (failed, model POM-collapse), two downsampling ablations (disagreed 54%, model not resolution-robust), a clustering-script sphericity inconsistency (two script versions gave non-comparable archetypes), and a candidate second Bnei Re'em replicate (`bnei_reem_samp_2_0`, root-caused to a missing crop, fixed, still 9.3× canonical POM) before Rony corrected the exclusion criterion itself and settled on Bnei Re'em n=1 (for POM) rather than pursuing that replicate further. |
| B | OM interface-ratio metrics + differentiating POM metrics (deep dive) | New dedicated chat | **Done, 2026-08-30.** Step 1 (`pom_analysis_20260815_light/` explained back to Rony) and step 2a (interface-ratio metrics: POM–pore, POM–matrix, SSA, IAD — `pom_interface_metrics_prompt.md`, run 2026-08-30, outputs in `05_evaluation/psd/pom_analysis_20260830_interface_metrics/`) both complete; the resolution/clustering side-quest that filled the middle of this track (steps 2026-08-23→08-30) was retired as not robust — see POM status summary near the top for both the interface-metrics headline result and the clustering retirement. Step 2b (shape/occlusion/spacing metrics beyond clustering) was not separately pursued once clustering itself was retired |
| C | Unified graphic language | Cowork skill `soil-figures-style` | Skill saved; apply when rebuilding figures |
| D | All image-derived metrics as graphs instead of tables | Final step, after A+B+E outputs and remote runs | Blocked by A, B, E, remote runs |
| E | Validate connectivity metrics (Euler χ, connectivity density, Γ, DA, tortuosity, χ(r)/crossover radius) and add to results | Separate chat / remote session (NOT part of Track B) | **Done and inserted into the draft, 2026-08-27.** `connectivity_topology_validation_prompt.md` ran 2026-08-25 (all 4 soils/resolutions, sign-convention reconciled — Bnei Re'em's positive χ is real, not a bug; tortuosity-NaN root-caused to a `porespy` solver non-convergence bug, not "no percolating path"). Follow-up `connectivity_crossover_radius_prompt.md` ran 2026-08-26: Vogel χ(r) connectivity function + crossover radius r* per soil — Rehovot ≈101 µm, Mishmar native (5.85 µm) ≈67 µm, Mishmar `Cu011_samp_2` (15 µm) ≈213 µm; **Bnei Re'em has no crossover in range (2–537 µm) — fragment-dominated at every resolvable scale**, consistent with its independently-lowest Γ (0.859) and smallest largest-component fraction (92.7%). Results written into **`figures draft - updated v7.docx`** as new Figure 4c (χ(r) plot) + Table 3 (Γ, r*, DA, raw χ per soil), inserted after Table 2 (former comment 3's location). No p-values/stats letters reported — flagged as n=1/soil, see "Open comments" below and comment 4 in the docx (reply to comment 3), which raises the sub-volume/REV pseudoreplication question for Maoz (Koestel, Larsbo & Jarvis 2020, *Geoderma*: no REV found for connectivity measures). **`connectivity_replicate_expansion_prompt.md` (drafted 2026-08-27, not yet run)** is the parallel practical answer — get real physical replicates instead of/alongside resolving the statistical question. **2026-08-29: Bnei Re'em's real replicate pool for this track is now known precisely** — 2 physical specimens, 2 usable pore-only reconstructions (canonical, `samp_2_0_recropped`) — see the correction note at the top of this file and `DATA_CATALOG.md`. Also still open per `DATA_CATALOG.md`: Mishmar's `Cu011_samp_1`/`samp_3` (raw/bootstrap-only, would need the full pipeline first) and Rehovot's `samp1` (also raw-only) as further potential Track E replicates once/if processed. Canonical data location: see "Key data files" |

## Remote Claude Code prompts (ready to paste, in project folder)

| File | Purpose | Status |
|---|---|---|
| `rehovot_inference_microsam_prompt.md` | 2 latest models × all Rehovot volumes + micro-SAM proofreading with disagreement queue | Not yet run |
| `fig4_psd_inference_prompt.md` | Replace Fig 4 voxel-χ² with subvolume ANOVA/KW + PERMANOVA + KS | **Run 2026-08-15** — results in `subvolume_psd_stats_summary_v1.md` (8 subvolumes/soil, KW+Dunn, PERMANOVA R²=0.842, KS object counts). Predates the Track A binning recommendation; re-examined under A2 — see Document state below (Fig 4b, `psd_A2_reanalysis_20260822/`) |
| `pom_conditioning_and_psd_prompt.md` | POM accessibility conditioning (denoised / pore-adjacent / connected-pore-adjacent) + POM size distribution | **Run 2026-08-15 in a "light" version** — outputs in `pom_analysis_20260815_light/` (Bnei Re'em + Mishmar: summary_pom_metrics.json, size distribution CSV/PNG, KS, midslice kept/removed PNGs, scripts/). Explained back to Rony in the Track B chat 2026-08-22; "light" skipped full-volume tifs and draft captions (see Track B row) |
| `pom_resolution_and_clustering_prompt.md` | 3-way Bnei Re'em / Mishmar-native / Mishmar-new-15µm comparison + per-object shape/spatial clustering | **Run 2026-08-23** — outputs in `pom_analysis_20260823_resolution/`. **Mishmar-new-15µm branch failed (POM segmentation) — see Track A row.** Bnei Re'em vs. Mishmar-native comparisons (Part A distance/size table minus that column; 2-soil clustering in `run_log_shape_clustering_2soil.txt`) remain valid |
| `mishmar_downsample_ablation_prompt.md` | Replaces the failed 08-23 branch: (1) label-downsample native Mishmar segmentation to ~15 µm, (2) downsample raw Mishmar CT then re-run `i2_loess` fresh at ~15 µm. 4-way comparison vs. Bnei Re'em + native Mishmar, branch-agreement check, fixes the pooled-scaler clustering bug | **Run 2026-08-24** — outputs in `pom_analysis_20260824_ablation/`. Both branches passed sanity checks but disagree with each other by ~54% — see Track A row for the full finding. Resolution-vs-soil-type question still open; clustering results not comparable to the 08-23 run due to a sphericity-method change between script versions (see Track A row) |
| `rehovot_table2_distance_metrics_prompt.md` | Rehovot pore-distance metrics | **Done** — results in `rehovot_distance_metrics_binary/`, already in v5+ Table 2 |
| `connectivity_topology_validation_prompt.md` | Track E: validate χ/connectivity-density/Γ, root-cause the NaN tortuosity, fill missing Rehovot topology run, add a resolution-matched (15 µm) Mishmar run, consolidated cross-soil table | **Run 2026-08-25** — results in `connectivity_validation_summary.md`. See Track E row. Short kickoff: `connectivity_topology_validation_kickoff.md` |
| `connectivity_crossover_radius_prompt.md` | Track E follow-up: Vogel χ(r) connectivity function + crossover radius r* | **Run 2026-08-26** — results in `crossover_radius_summary.md`. Inserted into `figures draft - updated v7.docx` (Fig 4c/Table 3) 2026-08-27 |
| `connectivity_replicate_expansion_prompt.md` | **Drafted 2026-08-27.** Fully autonomous: extends `DATA_CATALOG.md` with a pore-channel-specific validity note (separate from the POM gate), sweeps the share for uncatalogued Rehovot/Bnei Re'em volumes, runs the established Track E pipeline (topology + χ(r)) on every newly-identified usable binary/pore volume, and consolidates into a real multi-replicate (n≥2 where found) connectivity dataset per soil — the physical-replicate answer to comment 4's REV/pseudoreplication concern | **Not yet run.** The share sweep it called for has effectively now been done manually (2026-08-25 sweep + 2026-08-29 correction pass, both in `DATA_CATALOG.md`) — Bnei Re'em's real replicate pool (2 specimens / 2 reconstructions), Mishmar's (5 specimens, 2 more raw-only), and Rehovot's (3 specimens, 1 raw-only) are all known now. Running the actual Track E pipeline on the newly-identified usable volumes is still the open step |
| `DATA_CATALOG.md` | Not a remote prompt — the project's source-of-truth registry of every CT scan/sample: path, voxel size, preprocessing/model status, and validity with explicit reasoning under the corrected inclusion rule (exclude only for channel collapse / unfixed preprocessing defect / wrong model — not magnitude difference) | **Created 2026-08-24, rebuilt with real share-verified paths 2026-08-25/28, Bnei Re'em specimen count corrected 2026-08-29 (2 physical specimens, not the earlier n=1-with-uncertain-second-candidate picture).** Also carries the 2026-08-29 analysis-tier policy (structural work: all valid-pore volumes in scope; POM: exactly 3 named volumes). Read before any prompt does its own rediscovery |
| `pom_final_clustering_prompt.md` | **Run 2026-08-26** as `final`. Bnei Re'em (n=1, canonical `nlm_volume`) vs. Mishmar (n=2, both label-downsampled to ~15 µm) shape/spatial clustering with a pinned methodology: one fixed sphericity script version, minimum-resolvability cutoff (≥20 voxels/~300 µm diameter), scaler fit only on these 3 volumes | 6 archetypes found; one reproducible finding (Bnei Re'em absent from 2 archetypes both Mishmar replicates share), rest noisy — only 76/19/60 objects survive the resolvability cutoff per volume. See status summary above for full detail |
| `pom_roi_expansion_and_final_clustering_prompt.md` | **Drafted 2026-08-26, run 2026-08-29/30.** Fully autonomous (no check-ins): checks whether more raw volume exists beyond each volume's current crop, safely enlarges where there's real margin (automated sample-holder-safety + channel-collapse checks), reruns the pinned clustering methodology above, and separately checks whether the one reproducible finding holds at resolvability cutoffs of 15/20/25 voxels | **Done — outputs in `05_evaluation/psd/pom_analysis_20260829_roi_expansion/` (`final_report.md`).** Bnei Re'em's enlarged crop failed the channel-collapse check and fell back to its existing crop; both Mishmar crops enlarged successfully. The re-clustering collapsed to 2 archetypes and reversed the prior run's headline finding. **POM shape/archetype clustering retired 2026-08-30 as not robust** — see POM status summary near the top |
| `pom_interface_metrics_prompt.md` | **Drafted and run 2026-08-30.** Fully autonomous: POM–pore/POM–matrix voxel-face contact fractions (reproduction check against the pinned Bnei Re'em value), marching-cubes surface area + phase split, SSA, IAD (per-bulk-volume, Herring et al. 2015 convention), object-level interfacial-area concentration. Uses each volume's own pre-existing pinned noise-floor cutoff, NOT the ROI-expanded crops | **Superseded 2026-08-31 by the resolution fix below — see that row.** Outputs (kept as the documented bug record) in `05_evaluation/psd/pom_analysis_20260830_interface_metrics/` (`final_report.md`, per-soil `summary_pom_interface_metrics.json`, `comparison_table_raw.json`, figure). Part 1 reproduced the pinned Bnei Re'em value exactly. One flagged (investigated, resolved-as-not-a-bug) discrepancy, unaffected by the resolution fix: marching-cubes area came out smaller than voxel-face-count area for all 3 volumes, opposite the prompt's own stated literature expectation — validated via synthetic-sphere ground truth, documented in `final_report.md` |
| `pom_interface_metrics_resolution_fix_prompt.md` | **Drafted and run 2026-08-31.** Fully autonomous: fixes a resolution confound found in the row above — the original run's Mishmar `mean±SE (n=2)` mixed a 5.85µm replicate with a ~15µm replicate. Swaps in the already-existing label-downsampled-to-~15µm version of `mishmar_native` (reproduction check against its own pinned value), reruns Parts 1–4 unchanged, rebuilds the comparison table and figure, and reports old-vs-new side by side with an explicit per-metric survives/changes/reverses verdict | **Done — outputs in `05_evaluation/psd/pom_analysis_20260831_interface_metrics_resmatched/` (`README.md`, per-soil JSON, `comparison_table_raw.json`, figure) + a "Resolution correction" section appended to the original run's `final_report.md`.** Reference check for the swapped-in volume reproduced exactly (diff=0.0). **Every directional conclusion from the original run survived unchanged** — see POM status summary near the top for the corrected headline numbers. Residual (expected, reported not hidden) finding: the two Mishmar replicates' SSA still differ by ~34% even after resolution-matching, down from ~49% |

## Document state (v7)

Done: Fig 1d cumulative respiration (endpoint ANOVA, a/b/c letters); Fig 2b quotients qCO₂ + Cmic/Corg (paired per-jar; Loess lowest qCO₂ p=0.008; Vertisol lowest Cmic/Corg p=0.028); Table 1 dual bulk density mean ± SE; Table 2 mean (median) incl. Rehovot row; Fig 4b pore-size class fractions (A2 result — 3 functional classes, all significant p≤0.0004, PERMANOVA R²=0.55; caption hedges the non-robust Bnei Re'em–Mishmar 31–150 µm pair); **Fig 4c + Table 3, pore-network connectivity (Track E): χ(r) connectivity function plot, matched-resolution Γ/r*/DA/χ table across Rehovot/Bnei Re'em/Mishmar-15µm + Mishmar-native resolution check. No significance testing on this table — flagged explicitly in-document, see comment 4.**

**Latest file is now `figures draft - updated v7.docx`** (built from v6, which added nothing structurally beyond v5 — v6 and older are history).

Open comments in v7: (0) explain/resolve core-vs-bottle bulk density gap (Vertisol 1.35 vs 0.81 — candidate arbiter: CT-derived BD); (1) days 1–3 groups — two groups only if jar L2 excluded (Grubbs borderline G=1.151 vs 1.154; **check lab records for L2's day-3 titration**); (2) Fig 4 χ² replacement (Track A + remote prompt — done, see Fig 4b above); **(3) POM conditioning + connectivity Γ/Euler — connectivity half now answered (Fig 4c/Table 3 above); the POM shape/archetype clustering angle was tried (2026-08-26 pinned methodology, 2026-08-29/30 ROI-expansion follow-up) and retired 2026-08-30 as not robust; POM interface-ratio metrics (Track B) ran 2026-08-30 (`pom_interface_metrics_prompt.md`) — IAD differs 1.6–2.2× between soils despite indistinguishable SSA — see POM status summary near the top. Track B and comment 3 are now both closed; the only remaining open item in this comment thread is (4) below.** **(4) reply to (3):** flags to Maoz that connectivity metrics are n=1/soil (no replication) and that sub-volume splitting — the trick already used for PSD stats (Fig 4b) — is questionable specifically for connectivity metrics per Koestel, Larsbo & Jarvis (2020, *Geoderma*), who found no REV for connectivity measures (unlike porosity). Asks Maoz for direction: mixed model with sub-volume as random effect, or descriptive-only presentation. **`connectivity_replicate_expansion_prompt.md` is the parallel practical answer — get real physical replicates instead of/alongside resolving the statistical question, and as of 2026-08-29 the replicate inventory it needs is now known (see Track E row) — running the pipeline on those volumes is the remaining step. Blocking Track D's presentation of connectivity as a proper statistically-annotated figure until either this or the statistical question lands.**

**POM Table-2 numbers for Mishmar — resolved via descriptive/spatial statistics, see POM status summary near the top.** Volume-weighted median diameter (690.2 vs. 734.0 µm), largest-object share (17.1% vs. 45.1%), and Clark-Evans spatial clustering (R=0.640 Bnei Re'em vs. R=0.818 Mishmar, both aggregated but Bnei Re'em more strongly so) are unaffected by any of the clustering work and remain valid results from the original 2-soil comparison. **The shape/archetype-clustering extension of this work (run 2026-08-26, ROI-expansion follow-up 2026-08-29/30) was retired 2026-08-30 as not robust and is not part of the reportable results — see POM status summary near the top for detail.**

**Connectivity metrics (Euler χ, connectivity density, Γ, r*) — validated and now in the draft (v7, Fig 4c/Table 3).** All 4 reliability concerns from the original flag are resolved: (a) sign convention confirmed correct (Herring et al. 2015; χ/connectivity-density arithmetic reproduces each sample's physical volume exactly) — Bnei Re'em's positive χ is explained by χ(r), not a bug; (b) tortuosity NaN root-caused to a `porespy` finite-difference solver non-convergence bug, confirmed via captured exception on Bnei Re'em axis0 (~5h solve, `Exception: Solver failed to converge, exit code: 1000`) — genuinely not "no percolating path"; Mishmar-native's 3 NaNs strongly implicated as the same cause but not independently reconfirmed; (c) Rehovot topology run complete (χ=−53,704, Γ=0.989, porosity 0.305 matching existing Table 2); (d) resolution-matched run added (native 15 µm Mishmar, `Cu011_samp_2` pore channel, χ=−10,799, Γ=0.930). **χ(r) crossover radius (Vogel 1997/Roth 2001):** Rehovot/Bnei Re'em/Mishmar-15µm compared at matched ~15 µm resolution — Γ ranks Rehovot (0.989) > Mishmar (0.930) > Bnei Re'em (0.859); **Bnei Re'em's pore network is fragment-dominated across its entire measured range (r=2–537 µm, no loop-dominated regime at all)**, unlike the other two soils which show Vogel's expected loop-to-fragment transition at r*≈101 µm (Rehovot) and ≈213 µm (Mishmar-15µm) — three independent metrics (χ sign, Γ, r*) now agree Bnei Re'em's pore space is more fragmented than the other soils. **Open before this can carry full statistical annotation like the rest of the document:** all values are n=1/soil (single full-volume run); no p/letters reported; comment 4 in the docx asks Maoz whether a sub-volume/mixed-model approach is appropriate here given Koestel et al. (2020) found no REV for connectivity measures specifically; `connectivity_replicate_expansion_prompt.md` pursues the real-replicate alternative in parallel — now unblocked on the inventory side (Bnei Re'em's actual replicate pool is known as of 2026-08-29, see Track E row). Also: `Topology_Metrics_Aug2026` (no `V2`) on the network drive is the real/complete data location — see "Key data files".

Also open: Table 1 "Pending" cells (texture % — `Soil_Texture_Analysis.xlsx`, CaCO₃ — `Lime_CaCO3_Calcimeter_Experiment.xlsx`); Figure 5 (structure vs. activity — needs everything above); qCO₂ basal-window choice and days 1–3 post-hoc choice to confirm with Maoz.

## Starter prompt for Track B (paste into a new chat)

> Read PROJECT_STATUS.md in the project folder first. Track B deep dive, in this order:
> 1. **Explain last week's POM run to me first.** Go through `pom_analysis_20260815_light/` (all_soils_summary.json, per-soil summary_pom_metrics.json, pom_size_distribution.csv/png, KS json, midslice kept/removed PNGs, run_log.txt, scripts/) and the spec that produced it (`pom_conditioning_and_psd_prompt.md`). Walk me through: what conditioning was applied (denoised / pore-adjacent / connected-pore-adjacent), what cutoff was chosen and why, what the distance and size-distribution results say, and what "light" apparently skipped relative to the spec. I don't remember what happened there — assume nothing. **[Done 2026-08-22]**
> 2. Only after I confirm I understand: deepen — (a) metrics around ratios between organic-matter interfaces (POM–pore, POM–matrix, specific surface, interface density), (b) additional POM metrics that differentiate the soils (shape, occlusion, spacing…). Connectivity metrics (Γ, Euler) are NOT part of this track — they're Track E. **[Partially covered via the resolution/clustering side-quest — shape/spatial features run 2026-08-23/24/26 (`pom_resolution_and_clustering_prompt.md`, `mishmar_downsample_ablation_prompt.md`, `pom_final_clustering_prompt.md`); interface-ratio metrics (a) still open, next after ROI-expansion]**
> 3. End goal: an updated remote-run prompt + planned figures per the soil-figures-style skill. Remember: POM exists only for Bnei Re'em and Mishmar (Rehovot is binary); Mishmar is 5.85 µm voxels vs. 15 µm — mind resolution comparability (see Track A2 note).

## Kickoff prompts per track (paste into a new chat)

- **A2:** "Read `PROJECT_STATUS.md` — Track A row, 'Open follow-up (A2)' note, and the matching remote-prompts entry. Also read `psd_binning_recommendation.md` and `subvolume_psd_stats_summary_v1.md`. Execute A2: cross-soil inference restricted to ≥31 µm classes, drop resolution-confounded medians from cross-soil claims, robustness check with the native 15 µm Mishmar scan's pore channel (see 'Scans note')." — **done, see Fig 4b in Document state.**
- **A2 (POM clustering, ROI expansion):** **Done 2026-08-29/30 — see POM status summary near the top; clustering angle retired as not robust.**
- **B:** **Done 2026-08-30 — see POM status summary near the top and the Track B row.** Interface-ratio metrics (`pom_interface_metrics_prompt.md`) closed out the track.
- **D** (after A2+B+E): "Read `PROJECT_STATUS.md` — Track D row and 'Document state (v7)'. Execute Track D: rebuild all image-derived metrics as figures instead of tables, using the soil-figures-style skill, and insert into the latest figures draft."
- **Bulk density side item:** "Read `PROJECT_STATUS.md` — open comment (0): core-vs-bottle bulk density gap. Help me resolve it (CT-derived BD as arbiter) and draft the answer for the document."
- **E (replicate expansion):** "Read `PROJECT_STATUS.md` — Track E row (2026-08-29 update) and `DATA_CATALOG.md`'s Bnei Re'em/Mishmar/Rehovot sections. Execute `connectivity_replicate_expansion_prompt.md`: run the Track E pipeline on every newly-identified usable pore-only volume (Bnei Re'em's 2nd reconstruction, and any of Mishmar's `Cu011_samp_1`/`samp_3` or Rehovot's `samp1` worth processing first), consolidate a real multi-replicate connectivity dataset per soil."
- **C** needs no prompt — the `soil-figures-style` skill triggers automatically on any figure work.

## Scans note

**A native 15 µm Mishmar HaNegev scan exists** (`Cu011_samp_2`, scanned by Rony himself; the remote computer has it). Segmented with the `i2_loess` nnU-Net model on 2026-08-23. **Its pore channel is valid** (pore fraction 26.4%, consistent with native 5.85 µm Mishmar's 27.0%) — safe to use for any cross-soil pore/connectivity comparison affected by resolution (A2 medians, Track E). **Its POM channel failed** (POM fraction crashed to 0.120% vs. 1.613% native — the model did not recognize POM on this input) — do NOT use this scan's POM/Track-B results; the POM resolution question is being handled instead via computational ablation of the native 5.85 µm sample (`mishmar_downsample_ablation_prompt.md`). **For the full current Mishmar inventory (5 physical specimens, 2 more Cu011 scans beyond this one still raw/bootstrap-only) see `DATA_CATALOG.md`.**

## Key data files

Respiration: `Soil_CO2_Respiration_v4.xlsx` (Results sheet, per-jar). Cmic: `Fumigation_Extraction_Experiment.xlsx` (Calculations; replicates 1–4 pair with jars 1–4; Summary has Corg via LOI/1.724). Bulk density: `Bulk_Density_Experiment.xlsx` (core), `Organic carbon(2).xlsx` sheet "bulk density" (bottle). **Topology summaries — canonical location, network drive:** `Z:\Rony\remote_computer backup\Topology_Metrics_Aug2026\` (`raw/` for all 4 runs' `summary.json`/`psd_table.csv` etc. — Bnei Re'em, Mishmar native, Rehovot, Mishmar `Cu011_samp_2`; `connectivity_function/` for the χ(r) crossover CSVs/plot). **Corrected 2026-08-26** — the two local project-folder copies, `Topology_Metrics_Aug2026/` and `Topology_Metrics_Aug2026V2/`, are stale: both only contain the original 2 runs (Bnei Re'em, Mishmar native), missing the Rehovot and Mishmar-15µm runs and all `connectivity_function/` output. Don't source figures from either local copy — use the network path above, or resync the local copies before relying on them. Also: `rehovot_distance_metrics_binary/`. Analysis plan: `comments_insights_and_implementation_plan.md`. **Full scan inventory and locations for all 3 soils: `DATA_CATALOG.md` (canonical copy in this repo — see note at the top of this file about the third, out-of-scope network-share copy).**
