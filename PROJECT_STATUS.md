# Research Exercise — Mission Control

> Read this first in any new chat about the figures draft. It replaces re-deriving context from the long August conversation. Update the status tables here at the end of each working session.

## The project in one paragraph

Research exercise: impact of soil structure on microbial aspects. Three soils — Rehovot (brown-red sand), Bnei Re'em (Vertisol), Mishmar HaNegev (Loess). Lab side: respiration (NaOH traps, 15-day incubation, jar L4 excluded as outlier), fumigation-extraction Cmic (paired to respiration jars), bulk density (two methods), texture/CaCO₃ pending. Imaging side: microCT + nnUNet segmentation (deployed label convention: pore=5, POM=2 — NOT dataset_info.json; Rehovot is binary pore/solid, no POM), PSD via local thickness, distance-to-target topology metrics. Master document: **`figures draft - updated v6.docx`** (latest — edit from v6 only; older versions are history).

## Non-negotiable standards (mentor: dr. Maoz Dor)

1. **No SD anywhere. SE only.** Where SE is meaningless (voxel-wise metrics, n≈10⁸), report **mean (median)** instead. Already applied in v5 Tables 1–2.
2. A graph is not finished without statistics: omnibus p (+ effect size η²) printed on the figure, Tukey/LSD compact letters, plot EMM ± SE. Full text: `Maoz_Figure_Standard.md`.
3. Figure style: use the **`soil-figures-style`** skill (saved in Cowork) — fixed soil colors, ordering, and stats-annotation conventions.

## Work split (decided 2026-08-22)

| Track | What | Where | Status |
|---|---|---|---|
| A | PSD bin coarsening for multiple comparison — literature check | Subagent | **Done** — `psd_binning_recommendation.md`; bins merged into `fig4_psd_inference_prompt.md`. Note: Dor et al. 2025 is a Kravchenko-group (MSU) paper; 30–150 µm class verified from abstract, full table worth checking against the PDF. **Open follow-up (A2):** the subvolume run (see below) compared mean/median pore diameters across scans with different voxel sizes (Mishmar 5.85 µm vs. others 15 µm) — those medians are resolution-confounded (the fine scan sees small pores the coarse scans cannot; Mishmar's low medians, e.g. 52.8 µm subvolume / 23.5 µm object, are partly an artifact of this). Per the binning recommendation: restrict cross-soil inference to the ≥31 µm functional classes, and use the **native 15 µm Mishmar scan** (see "Scans note" below) — preferable to computational downsampling — as the robustness check for PORE metrics (its pore channel is valid — see Scans note; its POM channel is not). The per-class and PERMANOVA results need re-examination under this lens; whole-distribution medians should be dropped from cross-soil claims. **Extended 2026-08-22 (from the Track B chat, applies to POM not pore-PSD):** same voxel-size confound (Mishmar 5.85 µm vs. Bnei Re'em 15.00 µm) affects the POM distance/size numbers from `pom_analysis_20260815_light/`. Prompt drafted and run 2026-08-23 (`pom_resolution_and_clustering_prompt.md`, outputs in `pom_analysis_20260823_resolution/`) using the native 15 µm Mishmar scan (`Cu011_samp_2`) segmented with `i2_loess`. **Result: the POM segmentation on that scan failed** — POM voxel fraction crashed to 0.120% (vs. 1.613% native Mishmar, 0.818% Bnei Re'em) while pore fraction stayed normal (26.4%, in line with native Mishmar's 27.0%) — pore detection fine, POM detection failed, not a soil effect. All results derived from that branch are invalid and discarded: the "Mishmar new (15 µm)" column in `part_a_3way_comparison.md`, its row in `pom_spatial_pattern_summary.json`, and the 3-soil clustering run. **This also exposed a clustering-script bug:** the shared `StandardScaler` was pooled across all soils in a run, so the failed branch's bad features also corrupted Bnei Re'em's and native Mishmar's *own* clustering results in that same 3-soil run (Bnei Re'em: clean 2-soil run gave best_k=2/silhouette 0.811; contaminated 3-soil run gave best_k=6/silhouette 0.247 for the *same* objects). `pom_cluster_profiles.csv` on disk is the contaminated version — not trustworthy for any soil. **2026-08-24: replaced the failed new-scan approach with two computational ablations on the already-trusted `mishmar_native` (5.85 µm) sample** — label-downsample to ~15 µm (majority vote) and image-downsample-then-fresh-`i2_loess`-inference — prompt: `mishmar_downsample_ablation_prompt.md`, outputs in `pom_analysis_20260824_ablation/`. **Both branches passed the sanity check this time** (POM fraction 1.614% label-downsample / 0.856% image-then-predict, neither collapsed toward the 0.3% floor). **But the two branches strongly disagree with each other** (`part_c_4way_comparison.md`): distance-to-POM denoised mean is 347.3 µm (label-downsample, close to native Mishmar 268.1 µm) vs. 761.8 µm (image-then-predict, *above even Bnei Re'em's* 597.9 µm) — a 54% relative difference on the exact same physical sample at the same target resolution. N objects also diverges hard: 1726 (native) → 656 (label-downsample) → only 115 (image-then-predict). **Conclusion: the resolution-vs-soil-type question is still not cleanly answered, but a new, arguably more important finding emerged — the `i2_loess` segmentation model is not robust to how a 15 µm input is produced** (native rescan vs. downsampled image), which is itself a confound at least as large as the original soil-type question and needs its own sentence in the draft. The label-downsample branch (no new model inference, so methodologically cleaner) points toward the original gap being mostly a real soil-type effect, not resolution — but this is undercut by the image-then-predict branch showing the opposite pattern, so treat as suggestive, not settled. Spatial pattern (Clark-Evans) for both new branches came out "close to random" (R≈0.93–0.94) vs. native Mishmar's R=0.818 — likely an artifact of the collapsed object counts (656/115), not a real spatial-pattern change. **Clustering methodology inconsistency found:** the re-derived "clean" 2-soil archetype comparison in this run (`pom_archetype_crosssoil_comparison_2soil_clean.json`: 6 archetypes, χ²=826.6, p≈2×10⁻¹⁷⁶) does NOT match the 2026-08-23 "clean" run reported earlier (2 archetypes, χ²=12.5, p=0.0004) for the *same two soils* — not because of new data, but because the 08-24 script (`run_pom_shape_clustering_v2.py`) computes sphericity via marching-cubes while the 08-23 script used a simpler voxel-proxy method. **Pin one clustering script version going forward** — cluster/archetype results are not comparable across script versions. The 4-soil archetype comparison (`pom_archetype_crosssoil_comparison_4soil.json`) exists but is statistically fragile for `mishmar_image_then_predict` (only 115 objects spread across archetypes, several with single-digit counts) — don't lean on it. **2026-08-24, `pom_replicate_comparison_prompt.md` run:** dropped `mishmar_image_then_predict` entirely per the finding above; upgraded Mishmar to n=2 (native 5.85 µm + a second native ~8.8 µm sample, both label-downsampled to ~15 µm) but Bnei Re'em stayed at n=1 — a second candidate (`bnei_reem_samp_2_0`) was excluded for an implausible POM/pore fraction. Distance-to-POM denoised: Bnei Re'em 597.9 µm (n=1); Mishmar 347.3 and 500.6 µm (n=2, mean 423.9±76.7 µm, descriptive only) — the two Mishmar replicates differ by 44% from each other, and adding the second shifted the group mean 22% from the single-sample 08-24 figure, demonstrating real within-soil physical-sample variability. Largest-object share stayed nearly identical across both Mishmar replicates (45.4% both) despite the distance swing — POM's dominance by a few large fragments looks like a stable soil feature, unlike distance-to-POM. **`bnei_reem_samp_2_0` exclusion root-caused 2026-08-24:** initially suspected as a `Cu011_samp_2`-style wrong-model-branch failure; **ruled out** — checked directly against the inference log and driver script, confirmed the correct Bnei Re'em model/checkpoint (`multi_sample_fresh_bnei_reem_i4`, `checkpoint_final.pth`, same as the canonical trusted volume) was used. **Actual cause: `bnei_reem_samp_2_0` was never center-cropped before inference** — its preprocessed shape is the full uncropped 1344×896×1800 scan, versus the canonical Bnei Re'em's cropped 650×650×652 and every other volume in this project (all went through `crop_center_and_preprocess.py`); its driver script (`run_bnei_reem_samp_2_0_pipeline.py`) skips that step. The model likely saw substantial sample-holder/void/edge field-of-view it wasn't trained on — a mundane preprocessing gap, not a resolution or model-mismatch issue. No re-segmentation was warranted; Bnei Re'em correctly stays at n=1 for this run. **Open item if a real second Bnei Re'em replicate is wanted later:** re-run `bnei_reem_samp_2_0` through the proper center-crop step before inference. **2026-08-24: pursued and run — FAILED, sample stays excluded.** `bnei_reem_samp_2_0_repreprocess_prompt.md` audited the canonical pipeline (stack real slices → drop 12 SkyScan reconstruction-preview TIFFs also sitting in the raw folder → center-crop to 650³, matching the canonical `nlm_volume.tif` convention → norm200 → CUDA NLM → export), confirmed against a non-Bnei-Re'em volume too (Rehovot, via `colab_cli_runner.ipynb`). "mod 200" = norm200, a mode-based intensity rescale, not a padding/dimension step as originally guessed. Audit confirmed: norm200 and NLM both ran correctly the first time; **only the center-crop was skipped** (crop-skip theory holds, aux-file contamination theory ruled out — empirically scanned the actual 1800-slice preprocessed volume, no shape mismatch or statistical outliers). Re-ran the full sequence from the raw scan with a proper 650³ center-crop (script: `04_inference/scripts/run_bnei_reem_samp_2_0_recrop_pipeline.py`, output `bnei_reem_samp_2_0_recropped`) — hit and fixed a bug en route (the shared local `nlm_output/nlm_volume.tif` scratch slot held a 3-week-stale 650³ file from an unrelated sample that happened to match the target shape and false-positived a skip-if-exists check; caught via mtime, cleaned up, script fixed to never skip that step). **Sanity check (Part 3): FAIL.** Cropping helped a lot — pore fraction dropped from 1.82x to 1.31x of canonical (now plausible), POM dropped from 13.45x to 9.30x of canonical (7.62% vs. canonical's 0.82%) — but POM is still far too high to pass. Per the prompt's fail branch: not proceeding to Part 4; Bnei Re'em correctly stays at n=1; `pom_replicate_comparison_prompt.md`'s existing Part 4/5 results are unchanged. **Open question for next steps:** the crop-skip was real and partially explanatory but not the whole story — remaining candidates are (a) genuine physical/spatial POM heterogeneity between the two Bnei Re'em specimens (plausible for a Vertisol, given shrink-swell mixing), or (b) a scan-condition mismatch that couldn't be checked directly (the canonical `nlm_volume.tif`'s raw scan folder is no longer present on the share to compare scanner-log settings against `samp_2.0`'s, though both raw `bnei_reem_samp_2`/`bnei_reem_samp_2.0` folders share the same "highkV_cu011" scan settings as each other). Not investigated further this session. |
| B | OM interface-ratio metrics + differentiating POM metrics (deep dive) | New dedicated chat | **In progress.** Step 1 done — `pom_analysis_20260815_light/` explained back to Rony, confirmed understood. Note on what "light" skipped vs. the spec (`pom_conditioning_and_psd_prompt.md`): full-volume `distance_to_pom_*.tif` maps not kept (only midslice variants present), and the draft caption text the spec asked for was never produced. Conversation pivoted to the A2-style resolution-confound question for POM (see Track A row); that side-quest hit the segmentation failure above and is now being redone via `mishmar_downsample_ablation_prompt.md` before returning to steps 2–3 of the original Track B starter prompt (interface-ratio metrics; connectivity Γ/Euler is Track E, not this track) |
| C | Unified graphic language | Cowork skill `soil-figures-style` | Skill saved; apply when rebuilding figures |
| D | All image-derived metrics as graphs instead of tables | Final step, after A+B+E outputs and remote runs | Blocked by A, B, E, remote runs |
| E | Validate connectivity metrics (Euler χ, connectivity density, Γ) and add to results | Separate chat / remote session (NOT part of Track B) | **Prompt drafted 2026-08-24** — `connectivity_topology_validation_prompt.md` (+ short kickoff `connectivity_topology_validation_kickoff.md`). Not yet run. Covers: sign-convention/Γ reconciliation (Bnei Re'em χ>0 vs. Γ=0.86), NaN tortuosity root cause, missing Rehovot topology run, resolution-matched run on the native 15 µm Mishmar scan's **pore** channel (valid — see Scans note), consolidated cross-soil table |

## Remote Claude Code prompts (ready to paste, in project folder)

| File | Purpose | Status |
|---|---|---|
| `rehovot_inference_microsam_prompt.md` | 2 latest models × all Rehovot volumes + micro-SAM proofreading with disagreement queue | Not yet run |
| `fig4_psd_inference_prompt.md` | Replace Fig 4 voxel-χ² with subvolume ANOVA/KW + PERMANOVA + KS | **Run 2026-08-15** — results in `subvolume_psd_stats_summary_v1.md` (8 subvolumes/soil, KW+Dunn, PERMANOVA R²=0.842, KS object counts). Predates the Track A binning recommendation; re-examined under A2 — see Document state below (Fig 4b, `psd_A2_reanalysis_20260822/`) |
| `pom_conditioning_and_psd_prompt.md` | POM accessibility conditioning (denoised / pore-adjacent / connected-pore-adjacent) + POM size distribution | **Run 2026-08-15 in a "light" version** — outputs in `pom_analysis_20260815_light/` (Bnei Re'em + Mishmar: summary_pom_metrics.json, size distribution CSV/PNG, KS, midslice kept/removed PNGs, scripts/). Explained back to Rony in the Track B chat 2026-08-22; "light" skipped full-volume tifs and draft captions (see Track B row) |
| `pom_resolution_and_clustering_prompt.md` | 3-way Bnei Re'em / Mishmar-native / Mishmar-new-15µm comparison + per-object shape/spatial clustering | **Run 2026-08-23** — outputs in `pom_analysis_20260823_resolution/`. **Mishmar-new-15µm branch failed (POM segmentation) — see Track A row.** Bnei Re'em vs. Mishmar-native comparisons (Part A distance/size table minus that column; 2-soil clustering in `run_log_shape_clustering_2soil.txt`) remain valid |
| `mishmar_downsample_ablation_prompt.md` | Replaces the failed 08-23 branch: (1) label-downsample native Mishmar segmentation to ~15 µm, (2) downsample raw Mishmar CT then re-run `i2_loess` fresh at ~15 µm. 4-way comparison vs. Bnei Re'em + native Mishmar, branch-agreement check, fixes the pooled-scaler clustering bug | **Run 2026-08-24** — outputs in `pom_analysis_20260824_ablation/`. Both branches passed sanity checks but disagree with each other by ~54% — see Track A row for the full finding. Resolution-vs-soil-type question still open; clustering results not comparable to the 08-23 run due to a sphericity-method change between script versions (see Track A row) |
| `pom_replicate_comparison_prompt.md` | **New 2026-08-24.** Redoes the comparison from scratch, deliberately dropping `mishmar_image_then_predict` (unreliable, per the branch-disagreement finding above): uses ALL available Bnei Re'em scans as replicates, adds a second physical Mishmar volume (native voxel size a bit larger than 5.85 µm) label-downsampled to ~15 µm alongside the original, giving real replicate counts per soil for the first time instead of n=1. Clustering restricted strictly to these volumes (no image-then-predict, no discarded `Cu011_samp_2`) | Not yet run — starts with a discovery step (Part 0) to enumerate available Bnei Re'em scans and locate the second Mishmar volume, since neither is currently catalogued here |
| `rehovot_table2_distance_metrics_prompt.md` | Rehovot pore-distance metrics | **Done** — results in `rehovot_distance_metrics_binary/`, already in v5 Table 2 |
| `connectivity_topology_validation_prompt.md` | Track E: validate χ/connectivity-density/Γ, root-cause the NaN tortuosity, fill missing Rehovot topology run, add a resolution-matched (15 µm) Mishmar run, consolidated cross-soil table | **Drafted 2026-08-24** — not yet run. Short kickoff: `connectivity_topology_validation_kickoff.md` |
| `bnei_reem_samp_2_0_repreprocess_prompt.md` | Audits `bnei_reem_samp_2_0`'s driver script against the canonical preprocessing pipeline (NLM denoise, "mod 200" step, center-crop) for any other skipped steps beyond the confirmed missing crop; re-runs the full sequence properly; re-segments with the (already-confirmed-correct) Bnei Re'em model; sanity-checks against the canonical volume; if it passes, redoes the `pom_replicate_comparison_prompt.md` group comparison + clustering with Bnei Re'em at n=2 | **Run 2026-08-24 — sanity check failed.** Only the crop was missing (norm200/NLM ran fine); properly re-cropped + re-segmented as `bnei_reem_samp_2_0_recropped`, but POM fraction is still 9.3x canonical Bnei Re'em's (7.62% vs. 0.82%) even after the fix. Did not proceed to Part 4 — Bnei Re'em stays at n=1. See Track A row for full detail and open follow-up candidates |

## Document state (v5)

Done: Fig 1d cumulative respiration (endpoint ANOVA, a/b/c letters); Fig 2b quotients qCO₂ + Cmic/Corg (paired per-jar; Loess lowest qCO₂ p=0.008; Vertisol lowest Cmic/Corg p=0.028); Table 1 dual bulk density mean ± SE; Table 2 mean (median) incl. Rehovot row; Fig 4b pore-size class fractions (A2 result — 3 functional classes, all significant p≤0.0004, PERMANOVA R²=0.55; caption hedges the non-robust Bnei Re'em–Mishmar 31–150 µm pair).

**Latest file is now `figures draft - updated v6.docx`** (built from v5; v5 and older are history).

Open comments in v5: (0) explain/resolve core-vs-bottle bulk density gap (Vertisol 1.35 vs 0.81 — candidate arbiter: CT-derived BD); (1) days 1–3 groups — two groups only if jar L2 excluded (Grubbs borderline G=1.151 vs 1.154; **check lab records for L2's day-3 titration**); (2) Fig 4 χ² replacement (Track A + remote prompt); (3) POM conditioning + connectivity Γ/Euler (Track B + remote prompt; connectivity metrics not yet in any prompt).

**POM Table-2 numbers for Mishmar are still resolution-unresolved — now for a more interesting reason.** The 2026-08-23 attempt (new physical 15 µm scan) failed outright (segmentation failure). The 2026-08-24 retry (`mishmar_downsample_ablation_prompt.md`, two ablations on the native sample) passed its sanity checks but the two ablation branches disagree with each other by ~54% (347 µm label-downsample vs. 762 µm image-then-predict, vs. 598 µm Bnei Re'em / 268 µm native Mishmar) — see Track A row. Best current read: the methodologically cleaner branch (label-downsample, no new model inference) suggests the original gap is mostly a real soil-type effect, but this isn't settled given the other branch's contradiction, and the `i2_loess` model's own resolution-robustness is now a flagged issue worth a sentence in the draft regardless of which way the soil-type question resolves. **2026-08-24, second attempt:** `pom_replicate_comparison_prompt.md` redoes the comparison dropping the unreliable image-then-predict branch entirely, and upgrades from n=1-per-soil to real replicates — all available Bnei Re'em scans, plus a second physical Mishmar volume (native voxel size a bit larger than 5.85 µm) label-downsampled alongside the original. Not yet run. Volume-weighted median diameter (690.2 vs. 734.0 µm), largest-object share (17.1% vs. 45.1%), and Clark-Evans spatial clustering (R=0.640 Bnei Re'em vs. R=0.818 Mishmar, both aggregated but Bnei Re'em more strongly so) are unaffected by any of this and remain valid results from the original 2-soil comparison.

**Connectivity metrics (Euler χ, connectivity density, Γ) — exist but unvalidated, not yet in the draft.** Computed in the full extended runs (`Topology_Metrics_Aug2026V2/raw/psd_diag_*/summary.json`): Bnei Re'em χ=10,318, conn. density=−11.1 mm⁻³, Γ=0.859, DA=0.098; Mishmar χ=−65,340, conn. density=326.4 mm⁻³, Γ=0.927, DA=0.123. Reliability concerns before these enter results: (a) Bnei Re'em's *negative* connectivity density alongside a *positive* Euler number needs a sign-convention check (conn. density is usually derived from χ — the two rows may not use a consistent definition, or Bnei Re'em's pore network is genuinely dominated by isolated objects, which contradicts Γ=0.86); (b) tortuosity_axis0 is NaN in both runs; (c) no Rehovot values; (d) resolution gap (5.85 vs 15 µm) affects χ and conn. density strongly (small pores/connections) — use the native 15 µm Mishmar scan's pore channel (valid, see Scans note) for the cross-soil comparison. Validation = Track E; presentation as figures = Track D.

Also open: Table 1 "Pending" cells (texture % — `Soil_Texture_Analysis.xlsx`, CaCO₃ — `Lime_CaCO3_Calcimeter_Experiment.xlsx`); Figure 5 (structure vs. activity — needs everything above); qCO₂ basal-window choice and days 1–3 post-hoc choice to confirm with Maoz.

## Starter prompt for Track B (paste into a new chat)

> Read PROJECT_STATUS.md in the project folder first. Track B deep dive, in this order:
> 1. **Explain last week's POM run to me first.** Go through `pom_analysis_20260815_light/` (all_soils_summary.json, per-soil summary_pom_metrics.json, pom_size_distribution.csv/png, KS json, midslice kept/removed PNGs, run_log.txt, scripts/) and the spec that produced it (`pom_conditioning_and_psd_prompt.md`). Walk me through: what conditioning was applied (denoised / pore-adjacent / connected-pore-adjacent), what cutoff was chosen and why, what the distance and size-distribution results say, and what "light" apparently skipped relative to the spec. I don't remember what happened there — assume nothing. **[Done 2026-08-22]**
> 2. Only after I confirm I understand: deepen — (a) metrics around ratios between organic-matter interfaces (POM–pore, POM–matrix, specific surface, interface density), (b) additional POM metrics that differentiate the soils (shape, occlusion, spacing…). Connectivity metrics (Γ, Euler) are NOT part of this track — they're Track E. **[Partially covered via the resolution/clustering side-quest — shape/spatial features run 2026-08-23 in `pom_resolution_and_clustering_prompt.md`, but the cross-soil resolution question itself is still open after a segmentation failure, being retried via `mishmar_downsample_ablation_prompt.md`; interface-ratio metrics (a) still open]**
> 3. End goal: an updated remote-run prompt + planned figures per the soil-figures-style skill. Remember: POM exists only for Bnei Re'em and Mishmar (Rehovot is binary); Mishmar is 5.85 µm voxels vs. 15 µm — mind resolution comparability (see Track A2 note).

## Kickoff prompts per track (paste into a new chat)

- **A2:** "Read `PROJECT_STATUS.md` — Track A row, 'Open follow-up (A2)' note, and the matching remote-prompts entry. Also read `psd_binning_recommendation.md` and `subvolume_psd_stats_summary_v1.md`. Execute A2: cross-soil inference restricted to ≥31 µm classes, drop resolution-confounded medians from cross-soil claims, robustness check with the native 15 µm Mishmar scan's pore channel (see 'Scans note')."
- **A2 (POM resolution ablation):** "Read `PROJECT_STATUS.md` — Track A row, 2026-08-24 note. Fill in any remaining details in `mishmar_downsample_ablation_prompt.md` and execute it: label-downsample + image-downsample-then-predict ablations for Mishmar, 4-way comparison, fixed clustering."
- **B:** "Read `PROJECT_STATUS.md` and follow the 'Starter prompt for Track B' section — step 1 is done, continue with step 2 (interface-ratio metrics)."
- **D** (after A2+B+E): "Read `PROJECT_STATUS.md` — Track D row and 'Document state (v5)'. Execute Track D: rebuild all image-derived metrics as figures instead of tables, using the soil-figures-style skill, and insert into the latest figures draft."
- **Bulk density side item:** "Read `PROJECT_STATUS.md` — open comment (0): core-vs-bottle bulk density gap. Help me resolve it (CT-derived BD as arbiter) and draft the answer for the document."
- **E:** See `connectivity_topology_validation_kickoff.md` — paste that directly into a new chat. It points to the full spec in `connectivity_topology_validation_prompt.md`, which now covers the sign-convention/Γ check, NaN tortuosity root cause, the missing Rehovot run, and the resolution-matched Mishmar run (native 15 µm scan's pore channel, per the Scans note) in one pass.
- **C** needs no prompt — the `soil-figures-style` skill triggers automatically on any figure work.

## Scans note

**A native 15 µm Mishmar HaNegev scan exists** (`Cu011_samp_2`, scanned by Rony himself; the remote computer has it). Segmented with the `i2_loess` nnU-Net model on 2026-08-23. **Its pore channel is valid** (pore fraction 26.4%, consistent with native 5.85 µm Mishmar's 27.0%) — safe to use for any cross-soil pore/connectivity comparison affected by resolution (A2 medians, Track E). **Its POM channel failed** (POM fraction crashed to 0.120% vs. 1.613% native — the model did not recognize POM on this input) — do NOT use this scan's POM/Track-B results; the POM resolution question is being handled instead via computational ablation of the native 5.85 µm sample (`mishmar_downsample_ablation_prompt.md`).

## Key data files

Respiration: `Soil_CO2_Respiration_v4.xlsx` (Results sheet, per-jar). Cmic: `Fumigation_Extraction_Experiment.xlsx` (Calculations; replicates 1–4 pair with jars 1–4; Summary has Corg via LOI/1.724). Bulk density: `Bulk_Density_Experiment.xlsx` (core), `Organic carbon(2).xlsx` sheet "bulk density" (bottle). Topology summaries: `Topology_Metrics_Aug2026V2/raw/`, `rehovot_distance_metrics_binary/`. Analysis plan: `comments_insights_and_implementation_plan.md`.
