# Data Catalog — CT scans, segmentations, and validity

> Single source of truth for what scans exist, where, and their processing/validity status. Update this whenever a new scan is found, processed, or its validity is reassessed. Prompts should read this before doing their own rediscovery.

## Inclusion rule (revised 2026-08-24)

A sample is **excluded** only for one of these concrete reasons:
1. **Channel collapse** — a phase's voxel fraction crashes toward zero (e.g. POM <0.3%) while the other phase(s) stay in a normal range. This is the fingerprint of the model failing to recognize a class, not a real soil difference (seen in `Cu011_samp_2`).
2. **Known unfixed preprocessing defect** — e.g. missing center-crop, wrong model/checkpoint — until corrected.
3. **Wrong model/checkpoint used** for the soil (verified against the driver script + inference log, not assumed).

A sample is **NOT** excluded merely for differing in magnitude from another sample of the same soil — different physical specimens can genuinely differ (this was previously applied incorrectly to `bnei_reem_samp_2_0_recropped` and is corrected below). Magnitude differences get a note, not an exclusion.

## Full-share inventory sweep (2026-08-25)

Every soil's raw-scan location was swept end-to-end: the original SkyScan1272 acquisition folders under `\\hive3065\Yael_Mishael\Rony\` (one folder per scan session, each with a `.log` containing the true instrument-reported voxel size and reconstructed slice count) and the derived/processed volumes under `\\hive3065\Yael_Mishael\Rony\remote_computer backup\` (`10.5\`, `27.4\`, `nnUNet_resources\`). Two previously-uncatalogued physical scans were found (`bnei_reem_samp_2` and Mishmar `Cu011_samp_1`), one more Mishmar physical sample was confirmed and matched to its `.log` (`Cu011_samp_3`, previously only "bootstrapped" not catalogued), and Rehovot turned out to have a third physical sample (`samp1`) that has never been preprocessed at all. See per-soil tables below; raw-source paths are new columns.

**Total physical scans found: 11** (Rehovot 3, Bnei Re'em 3, Mishmar HaNegev 5). Previously catalogued: 6. Newly found/clarified this sweep: 5.

## Bnei Re'em (Vertisol) — 3 physical scans

| Sample ID | Type | Raw source (share) | Voxel size | Preprocessing | Model/checkpoint | Pore % | POM % | Status | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `bnei_reem_fresh_bnei_reem_i4` (canonical) | Native scan | **Raw acquisition folder no longer present on the share** — only the derived NLM volume (`10.5\nlm_volume.tif`, float32, shape 652×650×650) and annotation iterations (`fresh_train_annotations_bnei_reem\`) survive | 15.000149 µm | Full pipeline (stack → crop 650³ → norm200 → NLM) — confirmed complete | `multi_sample_fresh_bnei_reem_i4`, `checkpoint_final.pth` | 21.636 | 0.819 | **VALID — canonical reference** | Used in every prior comparison as "Bnei Re'em". Trained through iterations iter02→iter04 / i2→i4, all on this one physical scan (different annotation rounds, not different specimens) |
| `bnei_reem_samp_2_0` (original, uncropped) / `bnei_reem_samp_2_0_recropped` | Native scan, second physical Bnei Re'em specimen | `\\hive3065\Yael_Mishael\Rony\18.12.25 bnei_reem_samp_2.0\` — full reconstructed slice stack present (~1800 slices, 1344×896 px, 16-bit TIFF) + `.log` | 15.034357 µm (from `.log`; catalog previously rounded to "15 µm") | Full pipeline re-run correctly 2026-08-24 (stack real slices, drop 12 stray SkyScan preview TIFFs, crop 650³, norm200, NLM) | `multi_sample_fresh_bnei_reem_i4`, `checkpoint_final.pth` (confirmed correct) | 28.3 (1.31× canonical) | 7.62 (9.3× canonical — elevated, not collapsed) | **PENDING VISUAL REVIEW** | See `bnei_reem_samp_2_0_microsam_review_prompt.md`. Passes the numeric inclusion rule but Rony wants a micro-SAM visual check on the 9.3× POM gap before it counts as n=2 |
| `bnei_reem_samp_2` (no dot — **new find**) | Native scan, third physical Bnei Re'em specimen, distinct from `samp_2.0` above | `\\hive3065\Yael_Mishael\Rony\18.12.25 bnei_reem_samp_2\` — full reconstructed slice stack present (~1800 slices, 1344×896 px, 16-bit TIFF) + `.log`, no NRecon reconstruction log recorded in that folder (recon evidently done via a `_Rec` sub-run whose `.log` wasn't retained, but the sliced TIFFs are all present) | 15.000149 µm | **None** — no crop/norm200/NLM/inference of any kind found anywhere on the share for this raw folder | — none run — | — | — | **UNPROCESSED — raw only** | Not referenced by any script or `nnUNet_resources` subfolder. Note: this raw scan's voxel size (15.000149 µm) matches the value the catalog has been citing for the *canonical* volume, whose own raw folder is missing — worth checking by hand whether this is actually the canonical's original raw folder before assuming it's a genuinely unprocessed third specimen. Flagging both possibilities; not resolved by this sweep. If treated as a new specimen it would need: crop 650³ → norm200 → NLM → inference with `multi_sample_fresh_bnei_reem_i4` |

**Resolved:** the catalog's prior "are there other Bnei Re'em scans?" question — yes, one (`bnei_reem_samp_2`, no dot), fully raw and unprocessed. **Still open:** whether `bnei_reem_samp_2` is actually the missing canonical raw folder (misfiled/renamed) or a true third specimen — the canonical's own raw acquisition log could not be located anywhere on the share to compare directly.

## Mishmar HaNegev (Loess) — 5 physical scans

| Sample ID | Type | Raw source (share) | Voxel size | Preprocessing | Model/checkpoint | Pore % | POM % | Status | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `mishmar_native` = `mishmar_hanegev_maoz_3_5p85um` (canonical) | Native scan | `\\hive3065\Yael_Mishael\Rony\mishmar_hanegev_maoz\3-16mm_diam_5.85um\` — plain numbered slice TIFFs (no SkyScan `.log`; different acquisition source/software than the Cu011 sessions) | 5.85 µm | Full pipeline, confirmed. Processed volume `10.5\mishmar_hanegev_maoz_3_5p85um.tif` (1000³, float32) | `i2_loess`, `checkpoint_final.pth` | 27.038 | 1.615 | **VALID — canonical reference** | Multiple inference variants exist (`loess_i2`, `scratch`, `ablation_image_downsample`) in `nnUNet_resources\mishmar_hanegev_maoz_3_5p85um\` |
| `mishmar_hanegev_maoz_2_8p8um` (second Mishmar sample) | Native scan, second physical Mishmar specimen | `\\hive3065\Yael_Mishael\Rony\mishmar_hanegev_maoz\2-16mm_diam_8.8um\` — plain numbered slice TIFFs (no `.log`) | ~8.8 µm native → label-downsampled to ~15 µm for cross-soil comparison | Full pipeline, confirmed. Processed volume `10.5\mishmar_hanegev_maoz_2_8p8um.tif` (1000³, float32) | `i2_loess` (native-resolution inference, in-distribution) | plausible (see `pom_analysis_<date>/mishmar_label_downsample_2/`) | plausible | **VALID** | Used in `pom_replicate_comparison_prompt.md`, gave 500.6 µm distance-to-POM denoised (vs. 347.3 µm for native sample's downsample) |
| `Cu011_samp_2` | Native scan, third physical Mishmar specimen | `\\hive3065\Yael_Mishael\Rony\29.3.26 mishmar_hanegev_samp_2\` — SkyScan `.log` confirms 822 reconstructed slices, 1072×1072 px | 15.000149 µm native (log-confirmed) | Full pipeline (confirmed via log). NLM volume `10.5\mishmar_hanegev_Cu011_samp_2_Rec_nlm.tif` — full 822-slice height preserved, 650×650 in-plane crop | `i2_loess`, `checkpoint_final.pth` (confirmed correct model) | 26.4 (plausible) | 0.120 (**collapsed** — 0.074× canonical) | **POM channel INVALID** (channel-collapse). **Pore channel VALID** | Safe to reuse for pore-only work (A2, Track E connectivity); do not use for any POM/Track-B analysis |
| `Cu011_samp_1` (**new find** — was only "bootstrapped", not catalogued) | Native scan, fourth physical Mishmar specimen | `\\hive3065\Yael_Mishael\Rony\29.3.26 mishmar_hanegev_samp_1\` — SkyScan `.log` confirms 822 reconstructed slices, 1072×1072 px | 15.000149 µm native (log-confirmed) | **Partial only**: NLM-filtered + 650×650 in-plane crop exists but only for a 328-slice *subset* of the 822 (`10.5\mishmar_hanegev_Cu011_samp_1_Rec_nlm.tif`, shape 328×650×650) — full-height crop/norm200/NLM never run. Only a May-2026 "bootstrap" step exists: 3 hand-picked slices with pseudo-annotations (`10.5\bootstrap_outputs_fullctx\mishmar_hanegev_Cu011_samp_1_Rec_nlm\`), not a real segmentation | — none run — | — | — | **UNPROCESSED for full-volume use** | Needs: full-height (822-slice) crop 650³-style → norm200 → NLM → `i2_loess` inference, same as `Cu011_samp_2`/`Cu011_samp_3` |
| `Cu011_samp_3` (**new find** — was only "bootstrapped", not catalogued) | Native scan, fifth physical Mishmar specimen | `\\hive3065\Yael_Mishael\Rony\29.3.26 mishmar_hanegev_samp_3\` — SkyScan `.log` confirms 837 reconstructed slices, 860×860 px | 15.000149 µm native (log-confirmed) | **Partial only**: NLM-filtered + 650×650 in-plane crop exists but only for a 623-slice *subset* of the 837 (`10.5\mishmar_hanegev_Cu011_samp_3_Rec_nlm.tif`, shape 623×650×650) — full-height crop/norm200/NLM never run. Only the same May-2026 3-slice bootstrap pseudo-annotation step as `samp_1` | — none run — | — | — | **UNPROCESSED for full-volume use** | Needs: full-height (837-slice) crop → norm200 → NLM → `i2_loess` inference |
| `mishmar_label_downsample` (= native sample, computationally downsampled) | Derived (majority-vote label downsample of `mishmar_native`) | N/A — derived from `mishmar_native` | ~15 µm | N/A — label-space operation, not a new scan | N/A — reuses `mishmar_native`'s segmentation | 26.108 | 1.614 | **VALID**, methodologically clean | This is the "Mishmar sample 1" in `pom_replicate_comparison_prompt.md` |
| `mishmar_image_then_predict` | Derived (image downsample of `mishmar_native`'s raw CT, then fresh `i2_loess` inference) | N/A — derived from `mishmar_native` | ~15 µm | N/A | `i2_loess` on downsampled image (out-of-distribution input) | 25.632 | 0.856 | **EXCLUDED from clustering/comparison pool** | Disagrees with `mishmar_label_downsample` by 54% on the same physical sample — model-robustness finding, not a soil comparison result |

**Resolved:** Mishmar has **5 physical scans**, not 3. `Cu011_samp_1` and `Cu011_samp_3` exist on the share, are NLM-filtered only as partial-height subsets, and have never had a real (full-volume) segmentation run — only 3 pseudo-annotated slices each from a May 2026 bootstrap step. Both are candidate replicates once put through the full pipeline.

## Rehovot (sand) — 3 physical scans

| Sample ID | Type | Raw source (share) | Voxel size | Preprocessing | Model/checkpoint | Status | Notes |
|---|---|---|---|---|---|---|---|
| `Rehovot_samp1_highkV_Cu0.11_15um` (**new find** — never processed) | Native scan, first physical Rehovot specimen | `\\hive3065\Yael_Mishael\Rony\10.12.25 Rehovot\` — SkyScan `.log` confirms 808 reconstructed slices, 1276×1276 px | 15.034357 µm (log-confirmed) | **None** — no crop/norm200/NLM/inference found anywhere on the share | — none run — | **UNPROCESSED — raw only** | An orphaned duplicate copy of this scan's raw slices (1731 TIFFs, untitled) also sits in `\\hive3065\Yael_Mishael\Rony\test\` — same physical sample, not a separate one |
| `Rehovot_samp2_highkV_Cu0.11_15um` (canonical/used) | Native scan, second physical Rehovot specimen | `\\hive3065\Yael_Mishael\Rony\10.12.25_Rehovot_samp_2\` — SkyScan `.log` confirms 811 reconstructed slices, 1236×1236 px | 15.000149 µm (log-confirmed) | Full pipeline, cropped to 650³ (`10.5\rehovot_samp_2.tif`) | Both `multi_sample_fresh_bnei_reem_i4` and `i2_loess` run for cross-soil comparison (`nnUNet_resources\rehovot_inference_rehovot_samp_2\`) | **VALID** | Binary pore/solid — no POM class. Used in A2 pore-PSD work |
| `Rehovot_samp3_highkV_Cu0.11_15um` | Native scan, third physical Rehovot specimen | `\\hive3065\Yael_Mishael\Rony\10.12.25_Rehovot_samp_3\` (original Dec 2025 reconstruction) superseded by `10.12.25_Rehovot_samp_3_clean\` (corrected re-reconstruction, 2026-08-04) — `.log` confirms 811 reconstructed slices, 1236×1236 px | 15.034357 µm (log-confirmed) | Full pipeline, cropped to 650³ (`10.5\Rehovot_samp3_highkV_Cu0.11_15um.tif`, dated 2026-08-04, i.e. built from the "clean" reconstruction) | Both `multi_sample_fresh_bnei_reem_i4` and `i2_loess` run (`nnUNet_resources\rehovot_inference_Rehovot_samp3_highkV_Cu0.11_15um\`) | **VALID** | Same cross-soil dual-model setup as `samp2` |

**Resolved:** the catalog's prior "other Rehovot replicates" question. There are exactly 3 physical Rehovot scans; `samp1` has never been preprocessed at all — a genuine gap. `rehovot_inference_microsam_prompt.md` ("all Rehovot volumes") currently only has 2 of 3 covered.

## Known model branches (soil → training lineage)

- Bnei Re'em: `multi_sample_fresh_bnei_reem_i4` (trainer `nnUNetTrainer_betterIgnoreSampling_earlyStopValLoss_lowlr`, `checkpoint_final.pth`)
- Mishmar HaNegev / Loess: `i2_loess` (same trainer family — confirm exact folder name when next touching this)
- Rehovot: no dedicated model found on the share — both Rehovot samples were run through the Bnei Re'em (`..._i4`) and Mishmar (`i2_loess`) branches for cross-soil comparison; the catalog's earlier "Rehovot-specific model branch (confirm name)" note could not be confirmed by this sweep and should be treated as unconfirmed/likely incorrect until checked against the actual driver script

## Change log

- 2026-08-24: catalog created, seeded from the resolution/replicate investigation. `bnei_reem_samp_2_0_recropped` reinstated as valid under the revised inclusion rule (previously excluded incorrectly for differing in magnitude from canonical, not for an actual defect).
- 2026-08-25 (morning): `bnei_reem_samp_2_0_recropped` downgraded from "VALID — reinstated" to **PENDING VISUAL REVIEW**.
- 2026-08-25 (full-share sweep): rebuilt as a real inventory instead of an incidental list. Walked the original SkyScan acquisition folders (`\\hive3065\Yael_Mishael\Rony\*`) plus the derived-volume buckets (`10.5\`, `27.4\`, `nnUNet_resources\`). Found: a third, wholly unprocessed Bnei Re'em raw scan (`bnei_reem_samp_2`, no dot — possibly the missing canonical raw folder, unresolved); two Mishmar physical samples (`Cu011_samp_1`, `Cu011_samp_3`) that were only ever "bootstrapped" (3 pseudo-annotated slices each, partial-height NLM crop) and never actually segmented; a third physical Rehovot sample (`samp1`) that has never been preprocessed at all. Total physical scans across all three soils: 11 (previously 6 were catalogued). No inference, preprocessing, or comparison was run as part of this sweep — inventory only.
